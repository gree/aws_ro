require 'spec_helper'

describe Gree::Ec2Instance do
  it 'has a version number' do
    expect(Gree::Ec2Instance::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
